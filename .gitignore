# Xcode project is generated by XcodeGen — run `xcodegen generate`
*.xcodeproj
*.xcworkspace

# Build artifacts
DerivedData/
build/

# SPM
.build/
.swiftpm/

# macOS
.DS_Store

# xcode-build-server (VSCode SourceKit-LSP) — machine-specific paths
buildServer.json
